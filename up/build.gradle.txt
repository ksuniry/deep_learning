group = 'com.lt'
version = '0.0.1-SNAPSHOT'

apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'war'

sourceCompatibility = 1.8
targetCompatibility = 1.8


buildscript {
	ext {
		   rep_username = 'admin'
		   rep_password = 'nexusadmin'
		   rep_url 		= 'http://10.40.41.49:8100/nexus/content/groups/public'
	    }
}
    
repositories {
  maven {
    credentials {
       	username "${rep_username}"
        password "${rep_password}"
    }
    url "${rep_url}"
    allowInsecureProtocol true
  }
}    
    
///////////////////////////////////////////////

apply plugin: 'org.springframework.boot'
apply plugin: 'io.spring.dependency-management'

buildscript {
    repositories {
        maven {
            credentials {
                username "${rep_username}"
                password "${rep_password}"
            }
            url "${rep_url}"
            allowInsecureProtocol true
        }
    }
    
    dependencies {
        classpath group: 'org.springframework.boot'	, name: 'spring-boot-gradle-plugin', 	version: "2.1.5.RELEASE"
        //classpath group: 'io.freefair.gradle'	, name: 'lombok-plugin', 	version: "3.2.0"
        //classpath group: 'org.mybatis.spring.boot'	, name: 'mybatis-spring-boot-starter', 	version: "2.1.2"
    }
}

configurations {
	compileOnly {
		extendsFrom annotationProcessor
	}
}

dependencies {
	
	implementation group: 'org.springframework.boot'		, name: 'spring-boot-starter-web' ,version: "2.7.2"
			
    	// 내장된 tomcat에 jsp를 처리하는 서블릿
    	implementation 'org.apache.tomcat.embed:tomcat-embed-jasper'
		implementation 'javax.servlet:jstl'

		implementation 'org.springframework.boot:spring-boot-starter-thymeleaf'
		implementation 'nz.net.ultraq.thymeleaf:thymeleaf-layout-dialect'
	
		implementation 'org.jsoup:jsoup:1.15.3'
    	
	    implementation 'org.springframework.boot:spring-boot-starter-web'
	    implementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter:2.2.0'
	    implementation 'org.springframework.boot:spring-boot-starter-jdbc:2.6.5'
	    implementation 'org.projectlombok:lombok'
	    //developmentOnly 'org.springframework.boot:spring-boot-devtools'
	    implementation 'org.postgresql:postgresql'
	    annotationProcessor 'org.projectlombok:lombok'
	    
	    runtimeOnly 	'com.zaxxer:HikariCP:4.0.3'
	    
	    testImplementation('org.springframework.boot:spring-boot-starter-test') {
			exclude group: 'org.junit.vintage', module: 'junit-vintage-engine'
		}
		
	  	implementation 'org.apache.commons:commons-dbcp2'
}

