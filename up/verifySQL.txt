<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mvc.dao.VerifyDao">

	<select id="updVerifyStateStop"  resultType ="map" >
		UPDATE	TBL_FML
		SET		STATE = 'stop'
		WHERE 	STATE != 'comp'
	</select>

	<select id="selectVerifyFmlList"  resultType ="map"  parameterType="map" >
		SELECT 	* 
		FROM 	TBL_FML 
		WHERE 	1=1	
		AND		USE_YN = 'Y'
		<if test="check_chapter != null and !''.equals(check_chapter)">
		AND		(LAST_CHECK_CHAPTER <![CDATA[<]]>  #{check_chapter})
		</if>
		<if test="limit != null and !''.equals(limit)">
		LIMIT   #{limit}
		</if>
	</select>
	
</mapper> 